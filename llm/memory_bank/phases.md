# Phases

## Next
0. Run `/governance:migrate --plan` against a real pre-v0.3 repo. Never executed.
1. Decide whether v0.3.1 stands or is reverted and redone as a PR.
2. Reconcile the owner's local v0.2.0 clone with remote v0.3.1.
3. Resolve AD-G02: does this repo adopt its own governance?
4. Exercise the new memory-bank creation path in `establish` on a repo with no
   prior memory bank.
