# Memory bank — agentic-governance

Living project memory for the governance plugin itself. Read `activeContext.md`
first.

Note the recursion: this repo defines the governance other repos adopt, but does
not currently carry its own `docs/governance-delta.md`. See
`architecturalDecisions.md` AD-G02.
